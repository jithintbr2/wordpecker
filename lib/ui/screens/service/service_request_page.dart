import 'package:flutter/material.dart';

class ServiceRequestPage extends StatelessWidget {
  const ServiceRequestPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(title: Text('Service Request')), body: Container());
  }
}
