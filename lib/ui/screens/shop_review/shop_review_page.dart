import 'package:flutter/material.dart';

class ShopReview extends StatelessWidget {
  const ShopReview({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Reviews')),
    );
  }

  Widget _buildBody() {
    return Column(
      children: [],
    );
  }
}
